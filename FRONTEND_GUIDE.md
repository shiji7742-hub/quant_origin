# 前端页面使用指南

## 功能介绍

这个前端页面提供了一个直观的界面来查看筛选出的个股的技术面数据：

- **股票列表**：显示所有筛选出的候选股票，包括代码、最新价和涨跌幅
- **分时图**：展示最近5日的分时走势
- **日K线**：展示最近30日的日K线走势
- **股票信息**：显示股票的基本信息

## 安装依赖

```bash
pip install -r requirements.txt
```

需要的新依赖：
- flask>=2.3.0
- flask-cors>=4.0.0

## 运行方式

### 方式1：直接运行 Flask 应用

```bash
python app.py
```

然后在浏览器中打开：`http://localhost:5000`

### 方式2：使用 Gunicorn（生产环境）

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

## 使用流程

1. **打开页面**：访问 `http://localhost:5000`
2. **点击"开始扫描"**：系统会筛选符合条件的股票
3. **选择股票**：点击任意股票卡片查看详细数据
4. **查看图表**：
   - 左侧显示分时图（5日走势）
   - 右侧显示日K线（30日走势）
   - 下方显示股票基本信息

## API 接口

### 1. 扫描股票
```
GET /api/scan
```
返回筛选出的所有候选股票及其技术指标

### 2. 获取分时数据
```
GET /api/stock/<symbol>/intraday
```
返回指定股票的分时数据

### 3. 获取日K线
```
GET /api/stock/<symbol>/kline
```
返回指定股票的日K线数据

### 4. 获取股票信息
```
GET /api/stock/<symbol>/info
```
返回指定股票的基本信息

## 页面特性

- **响应式设计**：支持桌面和移动设备
- **实时图表**：使用 Chart.js 绘制专业图表
- **快速加载**：异步加载数据，不阻塞UI
- **美观界面**：现代化的渐变背景和卡片设计

## 常见问题

### Q: 分时数据为空？
A: 可能是因为股票在非交易时间或数据源暂时无法获取。请在交易时间重试。

### Q: 图表显示不正常？
A: 清除浏览器缓存或按 F12 打开开发者工具查看控制台错误信息。

### Q: 如何修改扫描条件？
A: 编辑 `screener.py` 中的 `screen_stocks()` 函数，修改筛选条件。

## 扩展功能建议

1. **添加更多技术指标**：在图表上显示 MA、MACD、RSI 等
2. **实时更新**：使用 WebSocket 实现实时数据推送
3. **导出功能**：支持导出图表和数据为 PDF/Excel
4. **预警功能**：设置价格预警和技术面预警
5. **对比分析**：支持多只股票对比分析
